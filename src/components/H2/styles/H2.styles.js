import styled from 'styled-components';
import { ssMainBlue } from '../../../shared/defaultStyleHelper';

export const ExtendedH2 = styled.h2`
  color: ${ssMainBlue};
  font-size: 2.2rem;
  font-weight: 400;
  text-transform: uppercase;
`;
