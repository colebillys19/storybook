import styled from 'styled-components';
import IconButton from '@material-ui/core/IconButton';

export const StyledPasswordIcon = styled(IconButton)`
  left: 0.8rem;
  padding: 0.8rem;
  position: relative;
`;
