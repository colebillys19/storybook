// import React from 'react';
import styled from 'styled-components';
import IconButton from '@material-ui/core/IconButton';

export const StyledIconButton = styled(IconButton)``;

// export const StyledIconButton = styled(IconButton)`
//   height: 3.4rem;
//   width: 3.4rem;
//   margin-left: ${({ adornmentPlacement }) => adornmentPlacement === 'start' ? '-0.7rem' : ''};
//   margin-right: ${({ adornmentPlacement }) => adornmentPlacement === 'start' ? '' : '-0.7rem'};
//   padding: 0;
// `;
