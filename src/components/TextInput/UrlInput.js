import React from 'react';
// import T from 'prop-types';

import BaseTextInput from './BaseTextInput';

const UrlInput = (props) => <BaseTextInput {...props} />;

UrlInput.propTypes = {};

UrlInput.defaultProps = {};

export default UrlInput;
