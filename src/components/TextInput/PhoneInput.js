import React from 'react';
// import T from 'prop-types';

import NumberInput from './NumberInput';

const PhoneInput = (props) => <NumberInput {...props} />;

PhoneInput.propTypes = {};

PhoneInput.defaultProps = {};

export default PhoneInput;
