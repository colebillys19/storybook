import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, select, text } from '@storybook/addon-knobs';

import GhostButtonLink from '../GhostButtonLink';

<Meta
  decorators={[withKnobs]}
  component={GhostButtonLink}
  title="Buttons|GhostButtonLink"
/>

<div style={{ position: 'relative', bottom: '20px' }}>

  ## GhostButtonLink

  A link meant to resemble a GhostButton on hover, behaves the same way as BaseLink.

  ### Props

  <div style={{ position: 'relative', bottom: '20px' }}>
    <Props of={GhostButtonLink} />
  </div>

</div>
