import styled from 'styled-components';

import { ssDarkGrey } from '../../utils/defaultStyleHelper';

const H2 = styled.h2`
  color: ${ssDarkGrey};
  font-size: 2.9rem;
  font-weight: 400;
  margin: 1.7rem 0;
`;

export default H2;
