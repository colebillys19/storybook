import { action } from '@storybook/addon-actions';
import { Button } from '@storybook/react/demo';
import { Story, Meta, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean, text } from '@storybook/addon-knobs/react';

<!--  --------------------- These are comments ---------------------------
  @description Meta Component
  @typedef (component) Storybook addon-docs block
  @props
  - title: (string) 'GROUP|STORY' Title is computed from the component's title, and matches the component caption in Storybook's navigation.
  - decorators: (array) An array of decorators
  - parameters: (object) An object of parameters
-->

### The Meta Tag

<Meta
  title='LEARN|MDX'
  decorators={[withKnobs]}
/>

<!--  --------------------- These are comments ---------------------------
  @description Story Component
  @typedef (component) Storybook addon-docs block
  @props
  - name: (string) 'name of f'
  - decorators: (array) An array of decorators
  - parameters: (object) An object of parameters
    @props(componentSubTitle)
-->
<Story name='nameOfStory'>
  <div>
    {text('text', 'default text')}
  </div>
</Story>