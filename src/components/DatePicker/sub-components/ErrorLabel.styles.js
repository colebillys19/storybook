import styled from 'styled-components';

import { muiRed } from '../../../utils/defaultStyleHelper';

export const DatePickerErrorLabel = styled.span`
  color: ${muiRed};
  font-size: 1.4rem;
  position: absolute;
  top: -2.1rem;
  left: 0.4rem;
`;
